#include <iostream>
#include <ostream>
#include <vector>

//#include "x-platform/utils.h"

int main(int argc, char *argv[]) {
  char buf[18] = {
      0,
  };

  std::cout << sizeof buf << std::endl;
  return 0;
}
